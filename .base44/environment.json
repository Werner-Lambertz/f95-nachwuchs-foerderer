{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "VITE_BASE44_APP_ID",
      "description": "Base44 app ID — identifies this app on the Base44 backend. Found in the Base44 Builder or the app's .env.local.",
      "requiredAtBoot": false
    },
    {
      "name": "VITE_BASE44_APP_BASE_URL",
      "description": "Base44 backend URL for this app (e.g. https://<app-id>.base44.app). Found in the Base44 Builder or the app's .env.local.",
      "requiredAtBoot": false
    }
  ]
}
